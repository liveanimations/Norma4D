<p id="notice"><%= notice %></p>

<h1>Listing Effects</h1>

<table>
  <thead>
    <tr>
      <th>Name ru</th>
      <th>Name en</th>
      <th>Version</th>
      <th>Description ru</th>
      <th>Description en</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @effects.each do |effect| %>
      <tr>
        <td><%= effect.name_ru %></td>
        <td><%= effect.name_en %></td>
        <td><%= effect.version %></td>
        <td><%= effect.description_ru %></td>
        <td><%= effect.description_en %></td>
        <td><%= link_to 'Show', effect %></td>
        <td><%= link_to 'Edit', edit_effect_path(effect) %></td>
        <td><%= link_to 'Destroy', effect, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Effect', new_effect_path %>
