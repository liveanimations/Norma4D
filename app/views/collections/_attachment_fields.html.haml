.row
  .col-md-12
    .nested-fields
      .form-group
        .col-md-10
          = f.object.file.file.try(:original_filename)
          = f.input :file, as: :file, label: false
        .col-md-2
          = link_to_remove_association 'Remove File', f, class: 'btn btn-danger'
